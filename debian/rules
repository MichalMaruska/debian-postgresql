#!/usr/bin/make -f

MAJOR_VER := 15
# -N libpq-dev -N libpq5
EXCLUDE_PACKAGES := -N libecpg6 -N libecpg-dev -N libecpg-compat3 -N libpgtypes3

include /usr/share/postgresql-common/server/postgresql.mk
