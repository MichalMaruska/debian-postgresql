#!/bin/zsh -feu

ref=$(sed -n -e 's|^ref = \([^ ]\+\) *$|\1|p' debian/upstream-source)
# fail if errors.

git archive --remote ~/git/postgresql/ $ref \
    | tar x

# so I should apply the patches too!
QUILT_PATCHES=debian/patches quilt push -a
# quilt push -av debian/patches/series
